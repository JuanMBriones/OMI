#include <bits/stdc++.h>
#define opt_io cin.tie(0);ios_base::sync_with_stdio(0);

using namespace std;

int main() {
	opt_io
    string s;
    getline(cin, s);
    for(int i=(s.size()-1); i>=0; i--) {
    	string c(i, ' ');
		cout << c;
    	for(int j=i; j<(s.size()); j++) {
    		cout << s[j];
    	}
    	cout << endl;
    }
    cin.get();
    return 0;
}