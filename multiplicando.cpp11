#include <bits/stdc++.h>

using namespace std;

int main() {
	long long int n, res = 1, i;
	cin >> n;
	if(n==0) {
		cout << 0 << endl;
		return 0;
	} 
	for(i=1; i<=n; i++) {
		res *= i; 
	}
	cout << res << endl;
	cin.get();
	return 0;
}