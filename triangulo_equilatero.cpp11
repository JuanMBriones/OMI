#include <bits/stdc++.h>

using namespace std;

int main() {
    int i, a, x[102] = {0};
    for(i=0; i<4; i++) {
        cin >> a;
        x[a]++;
    }
    sort(x, x+102);
    reverse(x, x+102);
    cout << ((x[0]>=3)? "1" : "0") << endl;
    cin.get();
    return 0;    
}