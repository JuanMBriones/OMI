#include <bits/stdc++.h>

using namespace std;

int main() {
	list<int> myList;
	int i, n, a;
	cin >> n;
	for(i=0; i<(2*n); i++) {
		cin >> a;
		myList.push_back(a);
	}
	myList.sort();
	myList.reverse();
	for(int n : myList) {
		cout << n << " ";
	}
	cout << endl;
	cin.get();
	return 0;
}