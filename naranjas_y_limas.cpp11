#include <bits/stdc++.h>
#define opt_io cin.tie(0);ios_base::sync_with_stdio(0);

using namespace std;

int main() {
    opt_io
    int i, n, m, par = 0, impar = 0;
    cin >> n;
    for(i=0; i<n; i++) {
        cin >> m;
        if(m%2) {
            impar++;
        }
        else {
            par++;
        }
    }
    cout << par << " " << impar << endl;
    cin.get();
    return 0;
}