#include <iostream>

using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    if(((a+b)%2)==0) {
        cout << (a+b) << endl << "PAR" << endl;    
    }
    else {
        cout << (a+b) << endl << "IMPAR" << endl;
    }
    cin.get();
    return 0;
}
