#include <iostream>

using namespace std;

long long int fact(int n) {
	if(n==0) {
		return 1;
	}
	else {
		return n*fact(n-1);
	}
}

int main() {
	int n;
	cin >> n;
	cout << fact(n) << endl;
	cin.get();
	return 0;
}