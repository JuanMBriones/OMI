#include <bits/stdc++.h>
#define opt_io cin.tie(0);ios_base::sync_with_stdio(0);

using namespace std;

int main() {
    opt_io
    int i, n, t, l;
    cin >> n;
    for(i=0; i<n; i++) {
        cin >> t >> l;
        if(!t==0) {
            cout << ((l%t==0)? "R" : "I") << endl;
        }
        else {
            cout << "R" << endl;
        }
    }
    cin.get();
    return 0;
}