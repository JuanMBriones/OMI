#include <bits/stdc++.h>
#define opt_io cin.tie(0);ios_base::sync_with_stdio(0);

using namespace std;

int main() {
	opt_io
	int n, m;
	cin >> n >> m;
	cout << n*(m-1)+1 << endl;
	cin.get();
	return 0;
}