#include <iostream>
#include <stack>

using namespace std;

int main() {
    int n, a;
    cin >> n;
    stack<int> s;
    for(int i=0; i<n; i++) {
        cin >> a;
        s.push(a);
    }
    while(!s.empty()) {
        cout << s.top() << endl;
        s.pop();
    }
    cin.get();
    return 0;
}