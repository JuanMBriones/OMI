#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    int x[10], i;
    for(i=0; i<10; i++) {
        cin >> x[i];

    }
    for(i=0; i<10 && x[i]!=*max_element(x, x+10); i++) {
        ;;
    }
    cout << *max_element(x, x+10) << " " << i << endl;
    cin.get();
    return 0;
}
